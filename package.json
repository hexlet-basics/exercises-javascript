{
  "scripts": {
    "test": "jest --colors --noStackTrace 2>&1"
  },
  "jest": {
    "testEnvironment": "node",
    "transform": {}
  },
  "dependencies": {
    "jest": "^23.5.0",
    "jest-cli": "^23.5.0",
    "power-assert": "^1.6.1"
  }
}
