---
name: Concatenación
tips:
  - >
    Si el editor contiene `// BEGIN` y `// END`, entonces el código debe
    escribirse entre estas líneas.
definitions:
  - name: Concatenación
    description: >
      la operación de unir dos cadenas. Por ejemplo, `console.log("King's " + '
      Landing');`
