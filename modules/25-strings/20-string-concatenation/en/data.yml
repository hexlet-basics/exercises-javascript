---
name: Concatenation
tips:
  - >
    If the editor contains `// BEGIN` and `// END`, then the code must be
    written between these lines.
definitions:
  - name: Concatenation
    description: >
      the operation of joining two strings. For example, `console.log("King's "
      + ' Landing');`
