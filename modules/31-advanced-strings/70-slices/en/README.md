Use `slice()` to extract part of a string.

```javascript
const text = 'Hello, World!';
console.log(text.slice(7));     // => World!
console.log(text.slice(7, 12)); // => World
console.log(text.slice(-6));    // => orld!  (negative index from end)
```
