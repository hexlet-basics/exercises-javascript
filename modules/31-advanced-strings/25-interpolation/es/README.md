La concatenación funciona, pero los template literals son más claros.

```javascript
const greeting = 'Hola';
const firstName = 'Ana';

console.log(`${greeting}, ${firstName}!`);
// => Hola, Ana!
```
