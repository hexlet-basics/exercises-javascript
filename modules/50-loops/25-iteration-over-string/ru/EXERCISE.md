Напишите функцию `maskCardNumber()`, которая скрывает номер банковской карты: заменяет все символы строки на `*`, кроме последних четырёх.

```javascript
maskCardNumber('1234567812345678'); // => '************5678'
maskCardNumber('12345678');         // => '****5678'
```
