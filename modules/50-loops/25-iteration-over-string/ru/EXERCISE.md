Напишите функцию `printReversedWordBySymbol(word)`, которая выводит символы слова в обратном порядке, каждый на отдельной строке.

```javascript
printReversedWordBySymbol('abc');
// c
// b
// a
```

Используйте цикл `for` с индексом и итерируйте от конца строки к началу.
