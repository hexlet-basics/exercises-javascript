Напишите функцию `encrypt(str)`, которая шифрует строку, меняя местами каждую пару соседних символов.

```javascript
encrypt('abcd'); // => 'badc'
encrypt('hello'); // => 'ehllo' (нечётный символ не меняется)
```

Используйте цикл `for` с шагом 2.
