Напишите функцию `multiplyNumbersFromRange(a, b)`, которая умножает все числа от `a` до `b` включительно и возвращает результат.

```javascript
multiplyNumbersFromRange(1, 4); // => 24   (1 * 2 * 3 * 4)
multiplyNumbersFromRange(2, 5); // => 120  (2 * 3 * 4 * 5)
```
