---

name: Инструкции (Statements)
theory: |
  Инструкция — это команда для компьютера. Код на PHP — это набор инструкций, которые, как правило, отделяются друг от друга символом `;`.

  Вот пример кода с двумя инструкциями.

  ```php
  <?php

  print_r('Mother of Dragons.');
  print_r('Dracarys!');
  ```

  При запуске этого кода на экран последовательно выводятся два предложения:

  ```
  Mother of Dragons.Dracarys!
  ```

  Теоретически инструкции можно написать последовательно друг за другом без переноса на новую строчку:

  ```php
  <?php

  print_r('Mother of Dragons.'); print_r('Drakarys!');
  ```

  Результат на экране будет таким же, но на практике такой подход считается плохим.

instructions: |
  Выведите на экран друг за другом три имени: `Robert`, `Stannis`, `Renly`. В результате на экране должно отобразиться:

  ```
  RobertStannisRenly
  ```

  Для каждого имени используйте свой собственный вызов `print_r`.

tips: []

definitions:
  - name: Инструкция (statement)
    description: "команда для компьютера, написанная на языке программирования. Код на PHP — это набор инструкций, отделенных (чаще всего) символом `;`."
