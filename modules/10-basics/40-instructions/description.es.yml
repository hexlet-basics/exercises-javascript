---

name: Instrucciones
theory: |
  Una instrucción es una orden para que la computadora realice algo. El código en JavaScript es un conjunto de instrucciones que generalmente se separan entre sí con el símbolo `;`.

  Aquí tienes un ejemplo de código con dos instrucciones.

  ```javascript
  console.log('Madre de Dragones.');
  console.log('Dracarys!');
  ```

  https://replit.com/@hexlet/js-basics-instructions

  Al ejecutar este código, se mostrarán en pantalla las dos frases de forma secuencial:

  <pre class='hexlet-basics-output'>
  Madre de Dragones.
  Dracarys!
  </pre>

  Teóricamente, las instrucciones se pueden escribir una tras otra sin saltar de línea:

  ```javascript
  console.log('Madre de Dragones.'); console.log('Drakarys!');
  ```

  El resultado en pantalla será el mismo, pero este tipo de código es incómodo de leer, por lo que las instrucciones se colocan una debajo de la otra.

  ¿Por qué es importante saber esto? Una instrucción es una unidad de ejecución. El intérprete, el programa que ejecuta el código en JavaScript, ejecuta las instrucciones estrictamente en orden. Como desarrolladores, debemos entender este orden y ser capaces de dividir mentalmente el programa en partes independientes que sean fáciles de analizar.

instructions: |
  Muestra en pantalla, uno tras otro, tres nombres: *Robert*, *Stannis*, *Renly*. El resultado en pantalla debe ser:

  <pre class='hexlet-basics-output'>
  Robert
  Stannis
  Renly
  </pre>

  Utiliza una llamada `console.log()` para cada nombre.

tips:
  - |
    [Un poco sobre los intérpretes](https://es.wikipedia.org/wiki/Int%C3%A9rprete)
  - |
    [Instrucciones y declaraciones por categorías](https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Statements)

definitions:
  - name: Intérprete
    description: |
      Un programa que ejecuta código en JavaScript.
  - name: Instrucción
    description: |
      Una orden para la computadora escrita en un lenguaje de programación. El código en JavaScript es un conjunto de instrucciones separadas (generalmente) por el símbolo `;`.