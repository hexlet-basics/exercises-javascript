Write a function `guessNumber(guess)` that:

- returns `'You win!'` if `guess === 42`
- returns `'Try again!'` otherwise

```javascript
guessNumber(42); // => 'You win!'
guessNumber(7);  // => 'Try again!'
```
