---
name: else
tips:
  - >
    [if...else](https://developer.mozilla.org/es/docs/Web/JavaScript/Reference/Statements/if...else)
definitions:
  - name: else
    description: >-
      una forma de definir un bloque de código que se ejecutará si la condición
      con `if` no se cumple
