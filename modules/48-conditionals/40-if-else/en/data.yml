---
name: else
tips:
  - >
    [if...else](https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Statements/if...else)
definitions:
  - name: else
    description: >-
      a way to define a block of code that will be executed if the condition
      with `if` is not satisfied
