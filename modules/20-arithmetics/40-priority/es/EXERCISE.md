Tú y 4 amigos (5 personas en total) pidieron 2 pizzas a 300 rublos cada una y 4 bebidas a 50 rublos cada una. Hay que dividir la cuenta en partes iguales.

Escribe un programa de una sola línea con `console.log()`, colocando los paréntesis de manera que primero se calcule el importe total y luego se divida entre todos:

```text
2 * 300 + 4 * 50 / 5  =  640  ← sin paréntesis la división se aplica solo a las bebidas
```

Salida esperada: `160`.
