Напишите функцию `isPensioner(age)`, которая возвращает `true`, если возраст `age` равен 60 или больше, и `false` в противном случае.

```javascript
isPensioner(65); // => true
isPensioner(30); // => false
```
