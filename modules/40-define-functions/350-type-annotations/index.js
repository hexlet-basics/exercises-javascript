// BEGIN
/**
 * @param {string} text
 * @param {number} repetitions
 * @returns {string}
 */
function wordMultiply(text, repetitions) {
  return text.repeat(repetitions);
}
// END

export default wordMultiply;
