Дана константа `temperature = 36.6`. Напишите программу, которая:

1. Отбрасывает дробную часть (`Math.trunc()`).
2. Преобразует результат в строку (`String()`).
3. Выводит строку с единицей измерения: `36 °C`.
