Посчитайте и выведите суммарную длину двух строк: `'Apple'` (5 символов) и `'Samsung'` (7 символов).

Используйте свойство `.length` для получения длины строки, затем сложите результаты.

```javascript
console.log('Apple'.length + 'Samsung'.length); // => 12
```
