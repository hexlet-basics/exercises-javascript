A website checks whether the ads of two partner companies will fit on one line. Their names are defined in the code - count the total number of characters in both names using the `length()` function and print it to the screen.

The function is already connected in the template:

```javascript
import { length } from 'hexlet-basics/string';
```
